import React from "react";
import Header from '@govuk-react/header';

const About = () => {
    return (
        <div>
            <Header level={1}>About component</Header>
        </div>
    );
};

export default About;