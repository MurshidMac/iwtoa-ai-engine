import React from "react";
import Header from '@govuk-react/header';

const Contact = () => {
    return (
        <div>
            <Header level={1}>Contact component</Header>
        </div>
    );
};

export default Contact;